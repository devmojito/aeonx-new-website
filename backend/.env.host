# Overrides for running manage.py from the host venv instead of inside the
# container: the service names db/minio only resolve on the compose network.
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5434
AWS_S3_ENDPOINT_URL=http://localhost:9000
