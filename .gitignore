# Stale Figma dump backup (kept locally only)
aeonx-node.json.stale-jun9

# Large Figma build inputs — kept locally, not tracked
aeonx-node.json
assets.zip

# Editor/build backups
index.html.bak
index.html.bak2
index.html.bak3
*.bak

# Local project context (contains Figma token) — do not commit
CLAUDE.md

__pycache__/
aeonx-mobile.json
_audit_expected.json
node_modules

# Blog publishing runbook (Webmin root URL, server IP, DB name) - do not commit
blog-fetch.md

# Harvested blog content pack (large, regenerable from the public site)
_blogdata.json

# Investor document library harvested from the public site (regenerable)
_invdocs.json

# Cached REST pulls for the homepage SaaS hero and the culture gallery dialog
# (regenerable with `--refetch`)
_saashero.json
_gallery.json
_saashero_variants.json

# Cached REST pull + scratch output for the mobile testimonials rebuild
_testimonials_mobile.json
_testimonials_mobile_out.html

# raw product reels handed over by the client: 1080p, ~150 MB each. The 720p
# encodes that actually ship live in assets/video/ and ARE tracked.
assets/videos/

# EC2 SSH private key -- never commit
*.pem

# Generated deliverables for the client, regenerated from source on demand
AeonX-Deployment-Architecture.pdf

# Agent worktrees: a full duplicate of the repo. Left untracked it also skews any
# page/asset count that walks the tree (it doubled the static-site figures once).
.claude/worktrees/
.vercel
.env*
